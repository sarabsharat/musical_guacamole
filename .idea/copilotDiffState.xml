<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/seed (snapshot).ts">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/seed (snapshot).ts" />
              <option name="originalContent" value="import &quot;dotenv/config&quot;;&#10;import {&#10;    PrismaClient,&#10;    Role,&#10;    CertStatus,&#10;    CertLevel,&#10;    RecipeStatus,&#10;    IngredientReference&#10;} from '@prisma/client';&#10;import { Pool } from 'pg';&#10;import { PrismaPg } from '@prisma/adapter-pg';&#10;import { initializeStorageBucket } from &quot;@/lib/s3-service&quot;;&#10;import * as fs from 'fs';&#10;import * as path from 'path';&#10;&#10;// Setup driver adapter for serverless/pg pooling environments&#10;const pool = new Pool({ connectionString: process.env.DATABASE_URL });&#10;const adapter = new PrismaPg(pool);&#10;const prisma = new PrismaClient({ adapter });&#10;&#10;// ==========================================&#10;// FUZZY MATCHING HELPER FUNCTIONS&#10;// ==========================================&#10;&#10;// Calculate Levenshtein distance between two strings&#10;function levenshteinDistance(str1: string, str2: string): number {&#10;    const len1 = str1.length;&#10;    const len2 = str2.length;&#10;    const matrix: number[][] = [];&#10;&#10;    // Initialize matrix&#10;    for (let i = 0; i &lt;= len1; i++) {&#10;        matrix[i] = [i];&#10;    }&#10;    for (let j = 0; j &lt;= len2; j++) {&#10;        matrix[0][j] = j;&#10;    }&#10;&#10;    // Fill matrix&#10;    for (let i = 1; i &lt;= len1; i++) {&#10;        for (let j = 1; j &lt;= len2; j++) {&#10;            const cost = str1[i - 1].toLowerCase() === str2[j - 1].toLowerCase() ? 0 : 1;&#10;            matrix[i][j] = Math.min(&#10;                matrix[i - 1][j] + 1,      // deletion&#10;                matrix[i][j - 1] + 1,      // insertion&#10;                matrix[i - 1][j - 1] + cost // substitution&#10;            );&#10;        }&#10;    }&#10;    return matrix[len1][len2];&#10;}&#10;&#10;// Calculate similarity score between two strings (0 to 1)&#10;function getSimilarity(str1: string, str2: string): number {&#10;    const maxLen = Math.max(str1.length, str2.length);&#10;    if (maxLen === 0) return 1.0;&#10;    const distance = levenshteinDistance(str1, str2);&#10;    return 1 - distance / maxLen;&#10;}&#10;&#10;// Find the best matching ingredient using fuzzy search&#10;function findBestMatchingIngredient(&#10;    searchTerm: string,&#10;    ingredientMap: Map&lt;string, IngredientReference&gt;,&#10;    threshold: number = 0.5&#10;): IngredientReference | undefined {&#10;    // Try exact match first (case-insensitive)&#10;    const exactMatch = Array.from(ingredientMap.keys()).find(&#10;        key =&gt; key.toLowerCase() === searchTerm.toLowerCase()&#10;    );&#10;    if (exactMatch) {&#10;        return ingredientMap.get(exactMatch);&#10;    }&#10;&#10;    // Try fuzzy matching&#10;    let bestMatch = null;&#10;    let bestScore = 0;&#10;    const searchTermLower = searchTerm.toLowerCase();&#10;&#10;    for (const [key, value] of ingredientMap) {&#10;        const keyLower = key.toLowerCase();&#10;&#10;        // Check if search term is contained in the ingredient name (substring match)&#10;        if (keyLower.includes(searchTermLower) || searchTermLower.includes(keyLower)) {&#10;            const score = Math.max(&#10;                searchTermLower.length / keyLower.length,&#10;                keyLower.length / searchTermLower.length&#10;            ) * 0.9; // Slightly penalize substring matches&#10;            if (score &gt; bestScore) {&#10;                bestScore = score;&#10;                bestMatch = value;&#10;            }&#10;        }&#10;&#10;        // Also check Levenshtein similarity&#10;        const simScore = getSimilarity(searchTerm, key);&#10;        if (simScore &gt; bestScore) {&#10;            bestScore = simScore;&#10;            bestMatch = value;&#10;        }&#10;    }&#10;&#10;    if (bestMatch &amp;&amp; bestScore &gt;= threshold) {&#10;        console.log(` Fuzzy matched &quot;${searchTerm}&quot; → &quot;${bestMatch.name}&quot; (${Math.round(bestScore * 100)}% similar)`);&#10;        return bestMatch;&#10;    }&#10;&#10;    return undefined;&#10;}&#10;&#10;// Get ingredient with fuzzy matching and helpful error messages&#10;const getIngredient = (searchTerm: string, ingredientMap: Map&lt;string, IngredientReference&gt;): IngredientReference =&gt; {&#10;    const ingredient = findBestMatchingIngredient(searchTerm, ingredientMap);&#10;&#10;    if (!ingredient) {&#10;        // Show similar ingredients for debugging&#10;        const allKeys = Array.from(ingredientMap.keys());&#10;        const suggestions = allKeys&#10;            .map(key =&gt; ({ key, score: getSimilarity(searchTerm, key) }))&#10;            .sort((a, b) =&gt; b.score - a.score)&#10;            .slice(0, 5)&#10;            .map(item =&gt; `  - ${item.key} (${Math.round(item.score * 100)}% match)`);&#10;&#10;        throw new Error(&#10;            `❌ Could not find ingredient matching &quot;${searchTerm}&quot;.\n` +&#10;            `Did you mean one of these?\n${suggestions.join('\n')}`&#10;        );&#10;    }&#10;    return ingredient;&#10;};&#10;&#10;// Shape of the seed JSON entries (food_db.json)&#10;interface SeedIngredient {&#10;    name: string;&#10;    name_ar?: string | null;&#10;    calories_per_g: number;&#10;    protein_per_g: number;&#10;    carbs_per_g: number;&#10;    fat_per_g: number;&#10;    allergens?: string[] | null;&#10;}&#10;&#10;async function main() {&#10;    console.log(` Seeding expanded localized testing dataset...`);&#10;    const filePath = path.join(__dirname, 'food_db.json');&#10;    const ingredients: SeedIngredient[] = JSON.parse(fs.readFileSync(filePath, 'utf-8')) as SeedIngredient[];&#10;    console.log(` Loading ${ingredients.length} ingredients from food_db.json...`);&#10;&#10;    // 1. Clean up existing records safely to prevent runtime seeding unique violations&#10;    await prisma.auditLog.deleteMany({});&#10;    await prisma.recipeVersion.deleteMany({});&#10;    await prisma.recipeIngredient.deleteMany({});&#10;    await prisma.recipe.deleteMany({});&#10;    await prisma.recipeDraft.deleteMany({});&#10;    await prisma.kitchenControlProfile.deleteMany({});&#10;    await prisma.ingredientReference.deleteMany({});&#10;    await prisma.restaurant.deleteMany({});&#10;    await prisma.user.deleteMany({});&#10;&#10;    console.log(' Cleaned database tables.');&#10;&#10;    // Initialize storage bucket for uploads&#10;    try {&#10;        await initializeStorageBucket();&#10;    } catch (err) {&#10;        console.error(&quot;❌ Failed to initialize storage bucket (uploads may not work until MinIO is reachable):&quot;, err);&#10;    }&#10;&#10;    // ==========================================&#10;    // 2. CREATE USERS (Platform Staff &amp; Authorities)&#10;    // ==========================================&#10;    await prisma.user.create({&#10;        data: { email: 'sara@platform.com', password_hash: '123456', role: Role.platform_admin, full_name: 'Sara', phone_number: '+962791111111' }&#10;    });&#10;&#10;    await prisma.user.create({&#10;        data: { email: 'rima@platform.com', password_hash: '123456', role: Role.platform_admin, full_name: 'Rima', phone_number: '+962792222222' }&#10;    });&#10;&#10;    await prisma.user.create({&#10;        data: { email: 'maha@jfda.gov.jo', password_hash: '123456', role: Role.nutritionist_auditor, full_name: 'Dr. Maha', phone_number: '+962793333333' }&#10;    });&#10;&#10;    await prisma.user.create({&#10;        data: { email: 'deema@jfda.gov.jo', password_hash: '123456', role: Role.jfda_officer, full_name: 'Deema Officer', phone_number: '+962794444444' }&#10;    });&#10;&#10;    // ==========================================&#10;    // 3. CREATE USERS (Restaurant Owners)&#10;    // ==========================================&#10;    const ownerLeen = await prisma.user.create({&#10;        data: { email: 'leen@dumplings.com', password_hash: '123456', role: Role.restaurant_owner, full_name: 'Leen', phone_number: '+962795555555' }&#10;    });&#10;&#10;    const ownerMira = await prisma.user.create({&#10;        data: { email: 'mira@morningcafe.com', password_hash: '123456', role: Role.restaurant_owner, full_name: 'Mira', phone_number: '+962796666666' }&#10;    });&#10;&#10;    const ownerAdam = await prisma.user.create({&#10;        data: { email: 'adam@leanburgers.com', password_hash: '123456', role: Role.restaurant_owner, full_name: 'Adam', phone_number: '+962797777777' }&#10;    });&#10;&#10;    const ownerDev = await prisma.user.create({&#10;        data: { email: 'dev-owner@localhost.com', password_hash: '123456', role: Role.restaurant_owner, full_name: 'Development Default Owner', phone_number: '+962790000000' }&#10;    });&#10;&#10;    console.log(' Seeded administrative and owner user accounts.');&#10;&#10;    // ==========================================&#10;    // 4. SEED INGREDIENT REFERENCES (Global Items)&#10;    // ==========================================&#10;    const ingredientMap = new Map&lt;string, IngredientReference&gt;();&#10;&#10;    for (const ingredient of ingredients) {&#10;        const created = await prisma.ingredientReference.upsert({&#10;            where: { name: ingredient.name },&#10;            update: {&#10;                // Prisma client uses camelCase for field names&#10;                nameAr: ingredient.name_ar ?? null,&#10;                calories_per_g: ingredient.calories_per_g,&#10;                protein_per_g: ingredient.protein_per_g,&#10;                carbs_per_g: ingredient.carbs_per_g,&#10;                fat_per_g: ingredient.fat_per_g,&#10;                allergens: ingredient.allergens || [],&#10;            },&#10;            create: {&#10;                name: ingredient.name,&#10;                nameAr: ingredient.name_ar ?? null,&#10;                calories_per_g: ingredient.calories_per_g,&#10;                protein_per_g: ingredient.protein_per_g,&#10;                carbs_per_g: ingredient.carbs_per_g,&#10;                fat_per_g: ingredient.fat_per_g,&#10;                allergens: ingredient.allergens || [],&#10;            },&#10;        });&#10;        ingredientMap.set(ingredient.name, created);&#10;    }&#10;&#10;    console.log(` Populated ${ingredientMap.size} analytical ingredient reference tables.`);&#10;&#10;    // Log a few ingredients to verify they're loaded&#10;    console.log(' Sample ingredients loaded:');&#10;    const sampleKeys = Array.from(ingredientMap.keys()).slice(0, 5);&#10;    sampleKeys.forEach(key =&gt; console.log(`  - ${key}`));&#10;&#10;    // ==========================================&#10;    // 5. CREATE RESTAURANTS &amp; DEPENDENT DATA&#10;    // ==========================================&#10;&#10;    // Fetch all ingredients into an array to pick from them randomly&#10;    const allIngredients = await prisma.ingredientReference.findMany();&#10;&#10;    const getRandomIngredients = (count: number) =&gt; {&#10;        const shuffled = [...allIngredients].sort(() =&gt; 0.5 - Math.random());&#10;        return shuffled.slice(0, count);&#10;    };&#10;&#10;    // --- Restaurant 1: The &quot;Perfect&quot; Case ---&#10;    const restLeen = await prisma.restaurant.create({&#10;        data: {&#10;            slug: 'leen-dumplings',&#10;            business_name: 'Leen Dumplings &amp; Breakfast',&#10;            address_line: 'Rainbow Street, Amman',&#10;            cert_status: CertStatus.ACTIVE,&#10;            cert_level: CertLevel.LEVEL_3,&#10;            owner_id: ownerLeen.id,&#10;            profile: { create: { hasDedicatedAllergenZones: true, usesStandardizedRecipes: true } }&#10;        }&#10;    });&#10;&#10;    // Get specific ingredients using fuzzy matching&#10;    const avocado = getIngredient('avocado', ingredientMap);&#10;    const tomato = getIngredient('tomato', ingredientMap);&#10;    const bread = getIngredient('bread', ingredientMap);&#10;&#10;    // Seed Recipe 1: Avocado Toast&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restLeen.id,&#10;            meal_name: 'Avocado Sun-Dried Tomato Toast',&#10;            image_url: 'https://images.unsplash.com/photo-1541532713592-79a0317b6b77?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Sliced avocado laid over warm sourdough toast with sun-dried tomatoes.',&#10;            calories: 320.00,&#10;            protein: 12.00,&#10;            carbs: 28.00,&#10;            total_fat: 18.00,&#10;            status: RecipeStatus.APPROVED,&#10;            ingredients: {&#10;                create: [&#10;                    {&#10;                        ingredient_id: avocado.id,&#10;                        user_stated_amount: '1 medium',&#10;                        normalized_grams: 150.0&#10;                    },&#10;                    {&#10;                        ingredient_id: tomato.id,&#10;                        user_stated_amount: '2 slices',&#10;                        normalized_grams: 30.0&#10;                    },&#10;                    {&#10;                        ingredient_id: bread.id,&#10;                        user_stated_amount: '2 slices',&#10;                        normalized_grams: 60.0&#10;                    }&#10;                ]&#10;            }&#10;        }&#10;    });&#10;&#10;    // Seed Recipe 2: Random ingredients for variety&#10;    const randomIngs = getRandomIngredients(3);&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restLeen.id,&#10;            meal_name: &quot;Chef's Special Salad&quot;,&#10;            image_url: 'https://images.unsplash.com/photo-1512621776951-a57141f2eefd?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Fresh seasonal greens with house vinaigrette.',&#10;            calories: 180.00,&#10;            protein: 8.00,&#10;            carbs: 15.00,&#10;            total_fat: 10.00,&#10;            status: RecipeStatus.APPROVED,&#10;            ingredients: {&#10;                create: randomIngs.map(ing =&gt; ({&#10;                    ingredient_id: ing.id,&#10;                    user_stated_amount: 'Standard Portion',&#10;                    normalized_grams: 80.0&#10;                }))&#10;            }&#10;        }&#10;    });&#10;&#10;    // --- Restaurant 2: The &quot;Newbie&quot; Case (Pending, Level 1) ---&#10;    const restMira = await prisma.restaurant.create({&#10;        data: {&#10;            slug: 'mira-morning-cafe',&#10;            business_name: 'Mira Morning Cafe',&#10;            address_line: 'Weibdeh, Amman',&#10;            cert_status: CertStatus.PENDING,&#10;            cert_level: CertLevel.LEVEL_1,&#10;            owner_id: ownerMira.id,&#10;            profile: {&#10;                create: {&#10;                    hasDedicatedAllergenZones: false,&#10;                    usesStandardizedRecipes: true&#10;                }&#10;            }&#10;        }&#10;    });&#10;&#10;    // Get specific ingredients using fuzzy matching&#10;    const coffee = getIngredient('coffee', ingredientMap);&#10;    const yogurt = getIngredient('yogurt', ingredientMap);&#10;    const honey = getIngredient('honey', ingredientMap);&#10;&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restMira.id,&#10;            meal_name: 'Iced Turkish Coffee',&#10;            image_url: 'https://images.unsplash.com/photo-1517701550927-30cf4ba1dba5?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Traditional unfiltered chilled coffee served over clear cracked ice with drops of cardamom.',&#10;            calories: 80.00,&#10;            protein: 1.00,&#10;            carbs: 15.00,&#10;            total_fat: 2.00,&#10;            status: RecipeStatus.PENDING,&#10;            ingredients: {&#10;                create: [&#10;                    {&#10;                        ingredient_id: coffee.id,&#10;                        user_stated_amount: '2 tablespoons',&#10;                        normalized_grams: 15.0&#10;                    }&#10;                ]&#10;            }&#10;        }&#10;    });&#10;&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restMira.id,&#10;            meal_name: 'Low Fat Greek Yogurt Parfait',&#10;            image_url: 'https://images.unsplash.com/photo-1488477181946-6428a0291777?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Strained low fat yogurt topped with organic mountain honey syrup and dried raisins.',&#10;            calories: 150.00,&#10;            protein: 15.00,&#10;            carbs: 20.00,&#10;            total_fat: 0.00,&#10;            status: RecipeStatus.REJECTED,&#10;            rejection_reason: 'Sugar content exceeds certified Level 1 threshold limits.',&#10;            ingredients: {&#10;                create: [&#10;                    {&#10;                        ingredient_id: yogurt.id,&#10;                        user_stated_amount: '1 cup',&#10;                        normalized_grams: 200.0&#10;                    },&#10;                    {&#10;                        ingredient_id: honey.id,&#10;                        user_stated_amount: '1 tablespoon',&#10;                        normalized_grams: 21.0&#10;                    }&#10;                ]&#10;            }&#10;        }&#10;    });&#10;&#10;    // --- Restaurant 3: The &quot;Trouble&quot; Case (Revoked) ---&#10;    const restAdam = await prisma.restaurant.create({&#10;        data: {&#10;            slug: 'adam-lean-burgers',&#10;            business_name: 'Adam 93 Lean Burgers',&#10;            address_line: 'Abdoun, Amman',&#10;            cert_status: CertStatus.REVOKED,&#10;            cert_level: CertLevel.LEVEL_2,&#10;            owner_id: ownerAdam.id,&#10;            profile: {&#10;                create: {&#10;                    hasDedicatedAllergenZones: false,&#10;                    usesStandardizedRecipes: false&#10;                }&#10;            }&#10;        }&#10;    });&#10;&#10;    // Get specific ingredients using fuzzy matching&#10;    const beef = getIngredient('beef', ingredientMap);&#10;    const lettuce = getIngredient('lettuce', ingredientMap);&#10;    // Reuse bread from earlier&#10;&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restAdam.id,&#10;            meal_name: '93% Lean Beef Burger',&#10;            image_url: 'https://images.unsplash.com/photo-1568901346375-23c9450c58cd?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Flame grilled lean ground brisket served on cross-cut whole wheat buns with fresh lettuce.',&#10;            calories: 450.00,&#10;            protein: 40.00,&#10;            carbs: 35.00,&#10;            total_fat: 15.00,&#10;            status: RecipeStatus.REVOKED,&#10;            rejection_reason: 'Random unannounced laboratory audit revealed real fat structural layers closer to 20%.',&#10;            ingredients: {&#10;                create: [&#10;                    {&#10;                        ingredient_id: beef.id,&#10;                        user_stated_amount: '200g patty',&#10;                        normalized_grams: 200.0&#10;                    },&#10;                    {&#10;                        ingredient_id: bread.id,&#10;                        user_stated_amount: '1 bun',&#10;                        normalized_grams: 80.0&#10;                    },&#10;                    {&#10;                        ingredient_id: lettuce.id,&#10;                        user_stated_amount: '2 leaves',&#10;                        normalized_grams: 20.0&#10;                    }&#10;                ]&#10;            }&#10;        }&#10;    });&#10;&#10;    console.log(`✅ Seeded 3 diverse relational restaurant datasets with structured recipe links, slugs, and kitchen profiles.`);&#10;&#10;    // --- Restaurant 4: Development Default Tenant ---&#10;    const restDevTenant = await prisma.restaurant.create({&#10;        data: {&#10;            slug: 'dev-tenant',&#10;            business_name: 'Development Test Restaurant',&#10;            address_line: 'localhost',&#10;            cert_status: CertStatus.ACTIVE,&#10;            cert_level: CertLevel.LEVEL_3,&#10;            owner_id: ownerDev.id,&#10;            profile: {&#10;                create: {&#10;                    hasDedicatedAllergenZones: true,&#10;                    usesStandardizedRecipes: true&#10;                }&#10;            }&#10;        }&#10;    });&#10;&#10;    // Add a test recipe for the dev tenant&#10;    const testIngs = getRandomIngredients(3);&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restDevTenant.id,&#10;            meal_name: 'Development Test Dish',&#10;            image_url: 'https://images.unsplash.com/photo-1546069901-ba9599a7e63c?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'A test dish for development purposes.',&#10;            calories: 250.00,&#10;            protein: 15.00,&#10;            carbs: 20.00,&#10;            total_fat: 12.00,&#10;            status: RecipeStatus.APPROVED,&#10;            ingredients: {&#10;                create: testIngs.map(ing =&gt; ({&#10;                    ingredient_id: ing.id,&#10;                    user_stated_amount: 'Test portion',&#10;                    normalized_grams: 100.0&#10;                }))&#10;            }&#10;        }&#10;    });&#10;&#10;    console.log(`✅ Seeded development default tenant for localhost testing (ID: ${restDevTenant.id}).`);&#10;&#10;    // Print summary statistics&#10;    const totalRecipes = await prisma.recipe.count();&#10;    const totalRestaurants = await prisma.restaurant.count();&#10;    console.log(` Summary: ${totalRestaurants} restaurants, ${totalRecipes} recipes, ${ingredientMap.size} ingredients seeded.`);&#10;}&#10;&#10;main()&#10;    .catch((e) =&gt; {&#10;        console.error(&quot;❌ Seeding execution failed: &quot;, e);&#10;        process.exit(1);&#10;    })&#10;    .finally(async () =&gt; {&#10;        await prisma.$disconnect();&#10;        await pool.end();&#10;    });" />
              <option name="updatedContent" value="import &quot;dotenv/config&quot;;&#10;import {&#10;    PrismaClient,&#10;    Role,&#10;    CertStatus,&#10;    CertLevel,&#10;    RecipeStatus,&#10;    IngredientReference&#10;} from '@prisma/client';&#10;import { Pool } from 'pg';&#10;import { PrismaPg } from '@prisma/adapter-pg';&#10;import { initializeStorageBucket } from &quot;@/lib/s3-service&quot;;&#10;import * as fs from 'fs';&#10;import * as path from 'path';&#10;&#10;// Setup driver adapter for serverless/pg pooling environments&#10;const pool = new Pool({ connectionString: process.env.DATABASE_URL });&#10;const adapter = new PrismaPg(pool);&#10;const prisma = new PrismaClient({ adapter });&#10;&#10;// ==========================================&#10;// FUZZY MATCHING HELPER FUNCTIONS&#10;// ==========================================&#10;&#10;// Calculate Levenshtein distance between two strings&#10;function levenshteinDistance(str1: string, str2: string): number {&#10;    const len1 = str1.length;&#10;    const len2 = str2.length;&#10;    const matrix: number[][] = [];&#10;&#10;    // Initialize matrix&#10;    for (let i = 0; i &lt;= len1; i++) {&#10;        matrix[i] = [i];&#10;    }&#10;    for (let j = 0; j &lt;= len2; j++) {&#10;        matrix[0][j] = j;&#10;    }&#10;&#10;    // Fill matrix&#10;    for (let i = 1; i &lt;= len1; i++) {&#10;        for (let j = 1; j &lt;= len2; j++) {&#10;            const cost = str1[i - 1].toLowerCase() === str2[j - 1].toLowerCase() ? 0 : 1;&#10;            matrix[i][j] = Math.min(&#10;                matrix[i - 1][j] + 1,      // deletion&#10;                matrix[i][j - 1] + 1,      // insertion&#10;                matrix[i - 1][j - 1] + cost // substitution&#10;            );&#10;        }&#10;    }&#10;    return matrix[len1][len2];&#10;}&#10;&#10;// Calculate similarity score between two strings (0 to 1)&#10;function getSimilarity(str1: string, str2: string): number {&#10;    const maxLen = Math.max(str1.length, str2.length);&#10;    if (maxLen === 0) return 1.0;&#10;    const distance = levenshteinDistance(str1, str2);&#10;    return 1 - distance / maxLen;&#10;}&#10;&#10;// Find the best matching ingredient using fuzzy search&#10;function findBestMatchingIngredient(&#10;    searchTerm: string,&#10;    ingredientMap: Map&lt;string, IngredientReference&gt;,&#10;    threshold: number = 0.5&#10;): IngredientReference | undefined {&#10;    // Try exact match first (case-insensitive)&#10;    const exactMatch = Array.from(ingredientMap.keys()).find(&#10;        key =&gt; key.toLowerCase() === searchTerm.toLowerCase()&#10;    );&#10;    if (exactMatch) {&#10;        return ingredientMap.get(exactMatch);&#10;    }&#10;&#10;    // Try fuzzy matching&#10;    let bestMatch = null;&#10;    let bestScore = 0;&#10;    const searchTermLower = searchTerm.toLowerCase();&#10;&#10;    for (const [key, value] of ingredientMap) {&#10;        const keyLower = key.toLowerCase();&#10;&#10;        // Check if search term is contained in the ingredient name (substring match)&#10;        if (keyLower.includes(searchTermLower) || searchTermLower.includes(keyLower)) {&#10;            const score = Math.max(&#10;                searchTermLower.length / keyLower.length,&#10;                keyLower.length / searchTermLower.length&#10;            ) * 0.9; // Slightly penalize substring matches&#10;            if (score &gt; bestScore) {&#10;                bestScore = score;&#10;                bestMatch = value;&#10;            }&#10;        }&#10;&#10;        // Also check Levenshtein similarity&#10;        const simScore = getSimilarity(searchTerm, key);&#10;        if (simScore &gt; bestScore) {&#10;            bestScore = simScore;&#10;            bestMatch = value;&#10;        }&#10;    }&#10;&#10;    if (bestMatch &amp;&amp; bestScore &gt;= threshold) {&#10;        console.log(` Fuzzy matched &quot;${searchTerm}&quot; → &quot;${bestMatch.name}&quot; (${Math.round(bestScore * 100)}% similar)`);&#10;        return bestMatch;&#10;    }&#10;&#10;    return undefined;&#10;}&#10;&#10;// Get ingredient with fuzzy matching and helpful error messages&#10;const getIngredient = (searchTerm: string, ingredientMap: Map&lt;string, IngredientReference&gt;): IngredientReference =&gt; {&#10;    const ingredient = findBestMatchingIngredient(searchTerm, ingredientMap);&#10;&#10;    if (!ingredient) {&#10;        // Show similar ingredients for debugging&#10;        const allKeys = Array.from(ingredientMap.keys());&#10;        const suggestions = allKeys&#10;            .map(key =&gt; ({ key, score: getSimilarity(searchTerm, key) }))&#10;            .sort((a, b) =&gt; b.score - a.score)&#10;            .slice(0, 5)&#10;            .map(item =&gt; `  - ${item.key} (${Math.round(item.score * 100)}% match)`);&#10;&#10;        throw new Error(&#10;            `❌ Could not find ingredient matching &quot;${searchTerm}&quot;.\n` +&#10;            `Did you mean one of these?\n${suggestions.join('\n')}`&#10;        );&#10;    }&#10;    return ingredient;&#10;};&#10;&#10;// Shape of the seed JSON entries (food_db.json)&#10;interface SeedIngredient {&#10;    name: string;&#10;    name_ar?: string | null;&#10;    calories_per_g: number;&#10;    protein_per_g: number;&#10;    carbs_per_g: number;&#10;    fat_per_g: number;&#10;    allergens?: string[] | null;&#10;}&#10;&#10;async function main() {&#10;    console.log(` Seeding expanded localized testing dataset...`);&#10;    const filePath = path.join(__dirname, 'food_db.json');&#10;    const ingredients: SeedIngredient[] = JSON.parse(fs.readFileSync(filePath, 'utf-8')) as SeedIngredient[];&#10;    console.log(` Loading ${ingredients.length} ingredients from food_db.json...`);&#10;&#10;    // 1. Clean up existing records safely to prevent runtime seeding unique violations&#10;    await prisma.auditLog.deleteMany({});&#10;    await prisma.recipeVersion.deleteMany({});&#10;    await prisma.recipeIngredient.deleteMany({});&#10;    await prisma.recipe.deleteMany({});&#10;    await prisma.recipeDraft.deleteMany({});&#10;    await prisma.kitchenControlProfile.deleteMany({});&#10;    await prisma.ingredientReference.deleteMany({});&#10;    await prisma.restaurant.deleteMany({});&#10;    await prisma.user.deleteMany({});&#10;&#10;    console.log(' Cleaned database tables.');&#10;&#10;    // Initialize storage bucket for uploads&#10;    try {&#10;        await initializeStorageBucket();&#10;    } catch (err) {&#10;        console.error(&quot;❌ Failed to initialize storage bucket (uploads may not work until MinIO is reachable):&quot;, err);&#10;    }&#10;&#10;    // ==========================================&#10;    // 2. CREATE USERS (Platform Staff &amp; Authorities)&#10;    // ==========================================&#10;    await prisma.user.create({&#10;        data: { email: 'sara@platform.com', password_hash: '123456', role: Role.platform_admin, full_name: 'Sara', phone_number: '+962791111111' }&#10;    });&#10;&#10;    await prisma.user.create({&#10;        data: { email: 'rima@platform.com', password_hash: '123456', role: Role.platform_admin, full_name: 'Rima', phone_number: '+962792222222' }&#10;    });&#10;&#10;    await prisma.user.create({&#10;        data: { email: 'maha@jfda.gov.jo', password_hash: '123456', role: Role.nutritionist_auditor, full_name: 'Dr. Maha', phone_number: '+962793333333' }&#10;    });&#10;&#10;    await prisma.user.create({&#10;        data: { email: 'deema@jfda.gov.jo', password_hash: '123456', role: Role.jfda_officer, full_name: 'Deema Officer', phone_number: '+962794444444' }&#10;    });&#10;&#10;    // ==========================================&#10;    // 3. CREATE USERS (Restaurant Owners)&#10;    // ==========================================&#10;    const ownerLeen = await prisma.user.create({&#10;        data: { email: 'leen@dumplings.com', password_hash: '123456', role: Role.restaurant_owner, full_name: 'Leen', phone_number: '+962795555555' }&#10;    });&#10;&#10;    const ownerMira = await prisma.user.create({&#10;        data: { email: 'mira@morningcafe.com', password_hash: '123456', role: Role.restaurant_owner, full_name: 'Mira', phone_number: '+962796666666' }&#10;    });&#10;&#10;    const ownerAdam = await prisma.user.create({&#10;        data: { email: 'adam@leanburgers.com', password_hash: '123456', role: Role.restaurant_owner, full_name: 'Adam', phone_number: '+962797777777' }&#10;    });&#10;&#10;    const ownerDev = await prisma.user.create({&#10;        data: { email: 'dev-owner@localhost.com', password_hash: '123456', role: Role.restaurant_owner, full_name: 'Development Default Owner', phone_number: '+962790000000' }&#10;    });&#10;&#10;    console.log(' Seeded administrative and owner user accounts.');&#10;&#10;    // ==========================================&#10;    // 4. SEED INGREDIENT REFERENCES (Global Items)&#10;    // ==========================================&#10;    const ingredientMap = new Map&lt;string, IngredientReference&gt;();&#10;&#10;    for (const ingredient of ingredients) {&#10;        const created = await prisma.ingredientReference.upsert({&#10;            where: { name: ingredient.name },&#10;            update: {&#10;                name_ar: ingredient.name_ar ?? '',&#10;                calories_per_g: ingredient.calories_per_g,&#10;                protein_per_g: ingredient.protein_per_g,&#10;                carbs_per_g: ingredient.carbs_per_g,&#10;                fat_per_g: ingredient.fat_per_g,&#10;                allergens: ingredient.allergens || [],&#10;            },&#10;            create: {&#10;                name: ingredient.name,&#10;                name_ar: ingredient.name_ar ?? '',&#10;                calories_per_g: ingredient.calories_per_g,&#10;                protein_per_g: ingredient.protein_per_g,&#10;                carbs_per_g: ingredient.carbs_per_g,&#10;                fat_per_g: ingredient.fat_per_g,&#10;                allergens: ingredient.allergens || [],&#10;            },&#10;        });&#10;        ingredientMap.set(ingredient.name, created);&#10;    }&#10;&#10;    console.log(` Populated ${ingredientMap.size} analytical ingredient reference tables.`);&#10;&#10;    // Log a few ingredients to verify they're loaded&#10;    console.log(' Sample ingredients loaded:');&#10;    const sampleKeys = Array.from(ingredientMap.keys()).slice(0, 5);&#10;    sampleKeys.forEach(key =&gt; console.log(`  - ${key}`));&#10;&#10;    // ==========================================&#10;    // 5. CREATE RESTAURANTS &amp; DEPENDENT DATA&#10;    // ==========================================&#10;&#10;    // Fetch all ingredients into an array to pick from them randomly&#10;    const allIngredients = await prisma.ingredientReference.findMany();&#10;&#10;    const getRandomIngredients = (count: number) =&gt; {&#10;        const shuffled = [...allIngredients].sort(() =&gt; 0.5 - Math.random());&#10;        return shuffled.slice(0, count);&#10;    };&#10;&#10;    // --- Restaurant 1: The &quot;Perfect&quot; Case ---&#10;    const restLeen = await prisma.restaurant.create({&#10;        data: {&#10;            slug: 'leen-dumplings',&#10;            business_name: 'Leen Dumplings &amp; Breakfast',&#10;            address_line: 'Rainbow Street, Amman',&#10;            cert_status: CertStatus.ACTIVE,&#10;            cert_level: CertLevel.LEVEL_3,&#10;            owner_id: ownerLeen.id,&#10;            profile: { create: { hasDedicatedAllergenZones: true, usesStandardizedRecipes: true } }&#10;        }&#10;    });&#10;&#10;    // Get specific ingredients using fuzzy matching&#10;    const avocado = getIngredient('avocado', ingredientMap);&#10;    const tomato = getIngredient('tomato', ingredientMap);&#10;    const bread = getIngredient('bread', ingredientMap);&#10;&#10;    // Seed Recipe 1: Avocado Toast&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restLeen.id,&#10;            meal_name: 'Avocado Sun-Dried Tomato Toast',&#10;            image_url: 'https://images.unsplash.com/photo-1541532713592-79a0317b6b77?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Sliced avocado laid over warm sourdough toast with sun-dried tomatoes.',&#10;            calories: 320.00,&#10;            protein: 12.00,&#10;            carbs: 28.00,&#10;            total_fat: 18.00,&#10;            status: RecipeStatus.APPROVED,&#10;            ingredients: {&#10;                create: [&#10;                    {&#10;                        ingredient_id: avocado.id,&#10;                        user_stated_amount: '1 medium',&#10;                        normalized_grams: 150.0&#10;                    },&#10;                    {&#10;                        ingredient_id: tomato.id,&#10;                        user_stated_amount: '2 slices',&#10;                        normalized_grams: 30.0&#10;                    },&#10;                    {&#10;                        ingredient_id: bread.id,&#10;                        user_stated_amount: '2 slices',&#10;                        normalized_grams: 60.0&#10;                    }&#10;                ]&#10;            }&#10;        }&#10;    });&#10;&#10;    // Seed Recipe 2: Random ingredients for variety&#10;    const randomIngs = getRandomIngredients(3);&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restLeen.id,&#10;            meal_name: &quot;Chef's Special Salad&quot;,&#10;            image_url: 'https://images.unsplash.com/photo-1512621776951-a57141f2eefd?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Fresh seasonal greens with house vinaigrette.',&#10;            calories: 180.00,&#10;            protein: 8.00,&#10;            carbs: 15.00,&#10;            total_fat: 10.00,&#10;            status: RecipeStatus.APPROVED,&#10;            ingredients: {&#10;                create: randomIngs.map(ing =&gt; ({&#10;                    ingredient_id: ing.id,&#10;                    user_stated_amount: 'Standard Portion',&#10;                    normalized_grams: 80.0&#10;                }))&#10;            }&#10;        }&#10;    });&#10;&#10;    // --- Restaurant 2: The &quot;Newbie&quot; Case (Pending, Level 1) ---&#10;    const restMira = await prisma.restaurant.create({&#10;        data: {&#10;            slug: 'mira-morning-cafe',&#10;            business_name: 'Mira Morning Cafe',&#10;            address_line: 'Weibdeh, Amman',&#10;            cert_status: CertStatus.PENDING,&#10;            cert_level: CertLevel.LEVEL_1,&#10;            owner_id: ownerMira.id,&#10;            profile: {&#10;                create: {&#10;                    hasDedicatedAllergenZones: false,&#10;                    usesStandardizedRecipes: true&#10;                }&#10;            }&#10;        }&#10;    });&#10;&#10;    // Get specific ingredients using fuzzy matching&#10;    const coffee = getIngredient('coffee', ingredientMap);&#10;    const yogurt = getIngredient('yogurt', ingredientMap);&#10;    const honey = getIngredient('honey', ingredientMap);&#10;&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restMira.id,&#10;            meal_name: 'Iced Turkish Coffee',&#10;            image_url: 'https://images.unsplash.com/photo-1517701550927-30cf4ba1dba5?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Traditional unfiltered chilled coffee served over clear cracked ice with drops of cardamom.',&#10;            calories: 80.00,&#10;            protein: 1.00,&#10;            carbs: 15.00,&#10;            total_fat: 2.00,&#10;            status: RecipeStatus.PENDING,&#10;            ingredients: {&#10;                create: [&#10;                    {&#10;                        ingredient_id: coffee.id,&#10;                        user_stated_amount: '2 tablespoons',&#10;                        normalized_grams: 15.0&#10;                    }&#10;                ]&#10;            }&#10;        }&#10;    });&#10;&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restMira.id,&#10;            meal_name: 'Low Fat Greek Yogurt Parfait',&#10;            image_url: 'https://images.unsplash.com/photo-1488477181946-6428a0291777?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Strained low fat yogurt topped with organic mountain honey syrup and dried raisins.',&#10;            calories: 150.00,&#10;            protein: 15.00,&#10;            carbs: 20.00,&#10;            total_fat: 0.00,&#10;            status: RecipeStatus.REJECTED,&#10;            rejection_reason: 'Sugar content exceeds certified Level 1 threshold limits.',&#10;            ingredients: {&#10;                create: [&#10;                    {&#10;                        ingredient_id: yogurt.id,&#10;                        user_stated_amount: '1 cup',&#10;                        normalized_grams: 200.0&#10;                    },&#10;                    {&#10;                        ingredient_id: honey.id,&#10;                        user_stated_amount: '1 tablespoon',&#10;                        normalized_grams: 21.0&#10;                    }&#10;                ]&#10;            }&#10;        }&#10;    });&#10;&#10;    // --- Restaurant 3: The &quot;Trouble&quot; Case (Revoked) ---&#10;    const restAdam = await prisma.restaurant.create({&#10;        data: {&#10;            slug: 'adam-lean-burgers',&#10;            business_name: 'Adam 93 Lean Burgers',&#10;            address_line: 'Abdoun, Amman',&#10;            cert_status: CertStatus.REVOKED,&#10;            cert_level: CertLevel.LEVEL_2,&#10;            owner_id: ownerAdam.id,&#10;            profile: {&#10;                create: {&#10;                    hasDedicatedAllergenZones: false,&#10;                    usesStandardizedRecipes: false&#10;                }&#10;            }&#10;        }&#10;    });&#10;&#10;    // Get specific ingredients using fuzzy matching&#10;    const beef = getIngredient('beef', ingredientMap);&#10;    const lettuce = getIngredient('lettuce', ingredientMap);&#10;    // Reuse bread from earlier&#10;&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restAdam.id,&#10;            meal_name: '93% Lean Beef Burger',&#10;            image_url: 'https://images.unsplash.com/photo-1568901346375-23c9450c58cd?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'Flame grilled lean ground brisket served on cross-cut whole wheat buns with fresh lettuce.',&#10;            calories: 450.00,&#10;            protein: 40.00,&#10;            carbs: 35.00,&#10;            total_fat: 15.00,&#10;            status: RecipeStatus.REVOKED,&#10;            rejection_reason: 'Random unannounced laboratory audit revealed real fat structural layers closer to 20%.',&#10;            ingredients: {&#10;                create: [&#10;                    {&#10;                        ingredient_id: beef.id,&#10;                        user_stated_amount: '200g patty',&#10;                        normalized_grams: 200.0&#10;                    },&#10;                    {&#10;                        ingredient_id: bread.id,&#10;                        user_stated_amount: '1 bun',&#10;                        normalized_grams: 80.0&#10;                    },&#10;                    {&#10;                        ingredient_id: lettuce.id,&#10;                        user_stated_amount: '2 leaves',&#10;                        normalized_grams: 20.0&#10;                    }&#10;                ]&#10;            }&#10;        }&#10;    });&#10;&#10;    console.log(`✅ Seeded 3 diverse relational restaurant datasets with structured recipe links, slugs, and kitchen profiles.`);&#10;&#10;    // --- Restaurant 4: Development Default Tenant ---&#10;    const restDevTenant = await prisma.restaurant.create({&#10;        data: {&#10;            slug: 'dev-tenant',&#10;            business_name: 'Development Test Restaurant',&#10;            address_line: 'localhost',&#10;            cert_status: CertStatus.ACTIVE,&#10;            cert_level: CertLevel.LEVEL_3,&#10;            owner_id: ownerDev.id,&#10;            profile: {&#10;                create: {&#10;                    hasDedicatedAllergenZones: true,&#10;                    usesStandardizedRecipes: true&#10;                }&#10;            }&#10;        }&#10;    });&#10;&#10;    // Add a test recipe for the dev tenant&#10;    const testIngs = getRandomIngredients(3);&#10;    await prisma.recipe.create({&#10;        data: {&#10;            restaurant_id: restDevTenant.id,&#10;            meal_name: 'Development Test Dish',&#10;            image_url: 'https://images.unsplash.com/photo-1546069901-ba9599a7e63c?auto=format&amp;fit=crop&amp;w=400&amp;q=80',&#10;            preparation_notes: 'A test dish for development purposes.',&#10;            calories: 250.00,&#10;            protein: 15.00,&#10;            carbs: 20.00,&#10;            total_fat: 12.00,&#10;            status: RecipeStatus.APPROVED,&#10;            ingredients: {&#10;                create: testIngs.map(ing =&gt; ({&#10;                    ingredient_id: ing.id,&#10;                    user_stated_amount: 'Test portion',&#10;                    normalized_grams: 100.0&#10;                }))&#10;            }&#10;        }&#10;    });&#10;&#10;    console.log(`✅ Seeded development default tenant for localhost testing (ID: ${restDevTenant.id}).`);&#10;&#10;    // Print summary statistics&#10;    const totalRecipes = await prisma.recipe.count();&#10;    const totalRestaurants = await prisma.restaurant.count();&#10;    console.log(` Summary: ${totalRestaurants} restaurants, ${totalRecipes} recipes, ${ingredientMap.size} ingredients seeded.`);&#10;}&#10;&#10;main()&#10;    .catch((e) =&gt; {&#10;        console.error(&quot;❌ Seeding execution failed: &quot;, e);&#10;        process.exit(1);&#10;    })&#10;    .finally(async () =&gt; {&#10;        await prisma.$disconnect();&#10;        await pool.end();&#10;    });" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>